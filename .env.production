# Production Convex deployment (public value — safe to commit).
# Points at the deployment that hosts the leaderboard functions
# (leaderboard_cache, crons). The leaderboard itself pulls historical focus
# data from tacit-clam-994 via the public REST API, so switching here does
# NOT lose any user history.
NEXT_PUBLIC_CONVEX_URL=https://dazzling-ocelot-629.convex.cloud
